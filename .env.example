# Copy this file to .env and adjust as needed
# Primary application database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cloud_db_inventory
# Test database used by pytest (override inside tests/test_api.py if needed)
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cloud_db_inventory_test
# Frontend dev (set in powershell before npm run dev)
VITE_API_URL=http://localhost:8000
